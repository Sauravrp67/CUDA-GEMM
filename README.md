# CUDA-GEMM

Focused CUDA GEMM playground extracted from `Attention_Variants`.

This repo keeps the same:

- CMake + setuptools extension build
- parity-test structure
- benchmark harness and report generation
- Nsight Compute / Nsight Systems profiling entrypoints

But narrows the scope to GEMM kernels only.
